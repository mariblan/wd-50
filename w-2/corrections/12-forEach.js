// ForEach
// On this array: ['one', 'two', 'three']
// Make a forEach loop and, at each turn, display
// the value in the console.

const myArray = ['one', 'two', 'three'];

myArray.forEach((item) => {
  console.log(item);
});
